export const a = "";
// const newMoves = Object.entries(moves).reduce((acc, [ direction, instructions ]) => {
// 	const calculatedPos = [calculateEnviromentPos(pos[0] + instructions[0]), calculateEnviromentPos(pos[1] + instructions[1])];

// 	if (enviroment[calculatedPos[0]][calculatedPos[1]].agent === TypeAgentEnum.PREDATOR) {
// 		acc = {
// 			...acc,
// 			[direction]: false,
// 		}

// 		blockDir = "";
// 	} else {
// 		acc = {
// 			...acc,
// 			[direction]: instructions,
// 		}

// 		positions[direction] = calculatedPos as [number, number];
// 	}

// 	return acc;
// }, {});
